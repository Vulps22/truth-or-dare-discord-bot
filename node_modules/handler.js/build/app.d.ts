import { Handler } from './handler';
export declare type RoutePath = string | RegExp | Array<string | RegExp>;
export interface Route {
    path: RoutePath;
    handler: Handler;
    match: any;
    method?: string;
}
export declare abstract class Application {
    protected _middlewares: Handler[];
    protected _routes: Route[];
    protected _addRoute(method: string, path: RoutePath, handler: Handler): void;
    protected _rewritePath(path: string): string;
}
